{
  "version": 2,
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "devCommand": "vite --port 3000",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
